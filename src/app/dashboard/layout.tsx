export const metadata = {
  title: 'Dashboard | Reactova',
  description: 'Reactova Admin Dashboard',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
